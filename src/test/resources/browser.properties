# WebDriver Browser (firefox, chrome, and ie)
browser=chrome

# Let webdriver run in headless mode
# NOTE headless is only available when using the chrome browser.
headless=false
# Application environments